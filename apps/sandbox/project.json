{
  "name": "sandbox",
  "projectType": "application",
  "sourceRoot": "apps/sandbox/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx http-server apps/sandbox/src -p 4200 -c-1 --silent"
      },
      "continuous": true
    }
  }
}
